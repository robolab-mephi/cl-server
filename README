Server for RN. Common LISP, sbcl(threads).

Dependencies: usocket

Installing:
--- load source from repository:
    git clone https://github.com/robolab-mephi/cl-server
--- change file config.lisp.example accordingly with your environment
--- rename file config.lisp.example in config.lisp
--- change file build.bat
--- run build.bat to create application coord.exe (both in Unix and Windows environments)
--- run coord.exe file
--- check system state from coordinator console by the command info:
	> info
	<node> <address>
	->	<program> <port>
	->	->	<device-type> <device-name>
--- run driver modules for devices (e.g. robotic platforms, range finders)
--- to stop coordinator, enter <quit> instruction
